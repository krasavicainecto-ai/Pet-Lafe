import random

kot2 = r"""
    /\     /\
   /  \___/  \
  (   ^   ^   )
   \    "    /
    \_______/
"""

dog_simple = r"""
  / \__
 (    @\___
 /         O
/   (_____/
/_____/   U
 Я собака!
"""

homak = """
  () ()
 ( . . )
 (") (")
"""




print('@@@@@@@@@@ Мой домашний питомец @@@@@@@@@@@@')

balans = 1000

inventory = []

print('Приветствую тебя в игре Тмамогочи!')
print('Сейчас вы выберете себе питомца: 1 - Кот, 2 - Собака, 3 - Хомяк')

pitomec = int(input('Введите цифру понравившегося питомца:'))


if pitomec == 1:
    print('Вы выбралт кота!')
    name = input('Выберете имя вашему питомцу:')
    print(f'Приветствуем вашего нового питомца по кличке: {name}')


elif pitomec == 2:
    print('Вы выбрали собаку!')
    name = input('Выберете имя вашему питомцу:')
    print(f'Приветствуем вашего нового питомца по кличке: {name}')



elif pitomec == 3:
    print('Вы выбрали Хомяк!')
    name = input('Выберете имя вашему питомцу:')
    print(f'Приветствуем вашего нового питомца по кличке: {name}')






print('Отлично, вы выбрали себе питомца и готовы теперь приступить к игре!')
balans = 1000 - 100
print(f'У вас на счету осталось {balans} рублей , 100 рублей потрачено было на вашего питомца!')



zdorovie = 100  # здоровье
golod = 50  # голод
nastroenie = 70  # настроение
energiya = 100  # энергия
chistota = 80  # чистота



while True:

    if pitomec == 1:
        print(kot2)
    elif pitomec == 2:
        print(dog_simple)
    elif pitomec == 3:
        print(homak)




    print(f"\nСостояние {name}:")
    print(f"❤️ Здоровье: {zdorovie}%")
    print(f"🍎 Голод: {golod}%")
    print(f"😊 Настроение: {nastroenie}%")
    print(f"⚡️ Энергия: {energiya}%")
    print(f"🛁 Чистота: {chistota}%")

    print("\n" + "="*30)
    print("Что вы хотите сделать?")
    print("1. Покормить (50 руб)")
    print("2. Поиграть (бесплатно)")
    print("3. Помыть (100 руб)")
    print("4. Отвести к ветеринару (200 руб)")
    print("5. Проверить состояние")
    print('6. Пойти в магазин')
    print('7. Мини игра (20 руб)')
    print('8. Смотреть в инвентарь')
    print('9. Смотреть баланс')
    print("10. Выйти из игры")




    deistvie = input("Выберите действие (1-8): ")
    if deistvie == '1':
        if balans >= 50:
            if golod < 100:
                golod -= 20
                balans -= 50
                chistota -= 10
                nastroenie += 10
                print(f'{name} был накормлен! Уровень голода уменьшился на 20%!')
                print(f'У вас осталось {balans} рублей!')

                golod = min(100, golod + 5)  # становится голоднее
                energiya = max(0, energiya - 3)  # устает
                chistota = max(0, chistota - 2)  # пачкается

                # Если голод > 80, здоровье падает
                if golod > 80:
                    zdorovie -= 5
                    print(f"⚠️ {name} слишком голоден! Здоровье уменьшается!")

                # Проверка на смерть
                if zdorovie <= 0 or golod >= 100:
                    print(f"💀 {name} умер! Игра окончена.")
                    break

    elif deistvie == '2':
        if energiya >= 20:
            if nastroenie < 100:
                nastroenie += 10
                energiya -= 30
                chistota -= 20
                balans += 50
                golod += 10
                print(f'{name} поиграл! Уровень настроения увеличился на 10%!')
                print(f' За то что вы поиграли вам + 50 рублей, Ваш баланс: {balans} рублей!')

                golod = min(100, golod + 5)  # становится голоднее
                energiya = max(0, energiya - 3)  # устает
                chistota = max(0, chistota - 2)  # пачкается

                # Если голод > 80, здоровье падает
                if golod > 80:
                    zdorovie -= 5
                    print(f"⚠️ {name} слишком голоден! Здоровье уменьшается!")

                # Проверка на смерть
                if zdorovie <= 0 or golod >= 100:
                    print(f"💀 {name} умер! Игра окончена.")
                    break

    elif deistvie == '3':
        if balans >= 100:
            print(f"{name} помыт! -100 рублей")
            balans -= 100
            chistota = min(100, chistota + 30)  # +30% чистоты, но не больше 100
            energiya = max(0, energiya - 10)
            print(f"Чистота: {chistota}%, Энергия: {energiya}%")
        else:
            print("Недостаточно денег!")

            # После всех действий, но ДО следующего цикла
            # Параметры ухудшаются со временем:
            golod = min(100, golod + 5)  # становится голоднее
            energiya = max(0, energiya - 3)  # устает
            chistota = max(0, chistota - 2)  # пачкается

            # Если голод > 80, здоровье падает
            if golod > 80:
                zdorovie -= 5
                print(f"⚠️ {name} слишком голоден! Здоровье уменьшается!")

            # Проверка на смерть
            if zdorovie <= 0 or golod >= 100:
                print(f"💀 {name} умер! Игра окончена.")
                break

    elif deistvie == '4':

        if balans >= 200:
            print(f"{name} у ветеринара! -200 рублей")
            balans -= 200
            zdorovie = min(100, zdorovie + 40)  # +40% здоровья
            print(f"Здоровье: {zdorovie}%")
        else:
            print("Недостаточно денег!")

            # После всех действий, но ДО следующего цикла
            # Параметры ухудшаются со временем:
            golod = min(100, golod + 5)  # становится голоднее
            energiya = max(0, energiya - 3)  # устает
            chistota = max(0, chistota - 2)  # пачкается

            # Если голод > 80, здоровье падает
            if golod > 80:
                zdorovie -= 5
                print(f"⚠️ {name} слишком голоден! Здоровье уменьшается!")

            # Проверка на смерть
            if zdorovie <= 0 or golod >= 100:
                print(f"💀 {name} умер! Игра окончена.")
                break

    elif deistvie == '5':

        print(f"\n=== СОСТОЯНИЕ {name} ===")
        print(f"💰 Баланс: {balans} рублей")
        print(f"❤️ Здоровье: {zdorovie}%")
        print(f"🍎 Голод: {golod}%")
        print(f"😊 Настроение: {nastroenie}%")
        print(f"⚡️ Энергия: {energiya}%")
        print(f"🛁 Чистота: {chistota}%")

        # После всех действий, но ДО следующего цикла
        # Параметры ухудшаются со временем:
        golod = min(100, golod + 5)  # становится голоднее
        energiya = max(0, energiya - 3)  # устает
        chistota = max(0, chistota - 2)  # пачкается

        # Если голод > 80, здоровье падает
        if golod > 80:
            zdorovie -= 5
            print(f"⚠️ {name} слишком голоден! Здоровье уменьшается!")

        # Проверка на смерть
        if zdorovie <= 0 or golod >= 100:
            print(f"💀 {name} умер! Игра окончена.")
            break


    elif deistvie == '6':


        print('Вы перешли в магазин!')
        print('1. Корм - 50 рублей')
        print('2. Игрушка - 100 рублей')
        print('3. Куртка меховая - 600 рублей')
        print('4. Вернуться назад')
        magazin = int(input('Введите цифру товара который хотите купить:'))
        if magazin == 1:
            if balans >= 50:
                balans -= 50
                inventory.append('Корм')
                print(f'Вы приобрели корм! Ваш баланс: {balans} рублей!')
                continue
        elif magazin == 2:
            if balans >= 100:

                balans -= 100
                inventory.append('Игрушка')
                print(f'Вы приобрели игрушку! Ваш баланс: {balans} рублей!')
                continue

        elif magazin == 3:
            if balans >= 600:
                balans -= 600
                inventory.append('Куртка меховая')
                print(f'Вы приобрели куртку! Ваш баланс: {balans} рублей')


        elif magazin == 4:
            print('Вы вернулись назад!')
            continue

    elif deistvie == "7":  # Мини-игра КНБ
        if balans >= 20:
            print("\n🎮 КАМЕНЬ-НОЖНИЦЫ-БУМАГА")
            print("Ставка: 20 рублей")
            print("Выигрыш: +50 рублей")
            print("1. Камень 👊")
            print("2. Ножницы ✌️")
            print("3. Бумага ✋")

            vibor = input("Твой выбор (1-3): ")

            # Проверяем что ввели цифру
            if vibor in ["1", "2", "3"]:
                # Компьютер выбирает
                komp = random.randint(1, 3)

                # Показываем выборы
                vibory = {"1": "Камень", "2": "Ножницы", "3": "Бумага"}
                print(f"\nТы: {vibory[vibor]}")
                print(f"Компьютер: {vibory[str(komp)]}")

                # Снимаем ставку
                balans -= 20

                # Проверяем кто выиграл
                if int(vibor) == komp:
                    print("🤝 НИЧЬЯ!")
                    print(f"Возвращаем 20 рублей")
                    balans += 20  # возвращаем деньги
                elif (vibor == "1" and komp == 2) or \
                        (vibor == "2" and komp == 3) or \
                        (vibor == "3" and komp == 1):
                    print("🎉 ТЫ ВЫИГРАЛ!")
                    balans += 50  # выигрыш
                    nastroenie = min(100, nastroenie + 20)
                    print(f"+50 рублей! Баланс: {balans} руб")
                    print(f"+20% настроения! Теперь: {nastroenie}%")
                else:
                    print("😢 Ты проиграл...")
                    nastroenie = max(0, nastroenie - 10)
                    print(f"-10% настроения... Теперь: {nastroenie}%")
            else:
                print("❌ Нужно ввести 1, 2 или 3!")
        else:
            print("❌ Недостаточно денег! Нужно 20 рублей.")





    elif deistvie == "8":
        if len(inventory) > 0:
            print('Ваш инвентарь:')
            for i, item in enumerate(inventory, 1):
                print(f'{i}. {item}')

            use = input('Использовать предмет? (1. да или 2. нет): ')
            if use.isdigit() and 1 <= int(use) <= len(inventory):
                item = inventory[int(use) - 1]
                if item == 'Корм':
                    golod = max(0, golod - 30)
                    print(f'{name} съел корм! Голод -30%')
                    inventory.remove('Корм')

                elif item == 'Игрушка':
                    energiya = max(0, energiya - 20)
                    nastroenie = max(0, nastroenie + 20)
                    print(f'{name} поиграл с игрушкой! Энергия - 20%, Настроение + 20%')

                elif item == 'Курька меховая':
                    zdorovie += 200
                    print(f'{name} купил и одел куртку! Здоровье + 200!')

        else:
            print('Ваш инвентарь пуст!')

    # Просто показ баланса:
    elif deistvie == "9":
        print(f"\n💰 БАЛАНС: {balans} руб")
        print(f"📦 Инвентарь: {len(inventory)} предметов")

        # Авто-подсказки:
        if golod > 70:
            print("⚠️ Питомец очень голоден!")
        if zdorovie < 30:
            print("⚠️ Питомец заболел! Нужен ветеринар!")
        if nastroenie < 30:
            print("⚠️ Питомец грустит!")
        continue

    else:
        print('Вы ввели число или слово которое не соответсвует предложенным числам!😒😒😒')
